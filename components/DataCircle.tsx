const DataCircle = ({ text }: { text: string }) => {
  return (
    <div className="crime-datacircle crime-pos-flex crime-pos-center-center">
      <span>{text}</span>
    </div>
  );
};

export { DataCircle };
